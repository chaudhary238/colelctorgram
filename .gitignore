.claude
node_modules/
.env
.env.local
.env.*
!.env.example
.prod.env
*.prod.env
dist/
build/

# Seed scripts — carry demo/seed data + are run with env-provided DB creds.
# Kept off the public repo (not needed by the deploy image; run locally).
backend/seed_dev_data.py
backend/seed_social_graph.py

# Standalone data ingestion pipelines — local tooling, not part of the deploy image.
/ingestion/
